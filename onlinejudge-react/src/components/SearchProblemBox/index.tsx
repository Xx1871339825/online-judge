import {FC, ReactElement} from 'react';

interface IProps{

}

const SearchProblemBox:FC<IProps> = (props):ReactElement => {
    return (
        <div>

        </div>
    );
};

export default SearchProblemBox;
