import {FC, ReactElement} from 'react';

interface IProps{

}

const About:FC<IProps> = (props):ReactElement => {
    return (
        <div>
            这是关于页面
        </div>
    );
};

export default About;
